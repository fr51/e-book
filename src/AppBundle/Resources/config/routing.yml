App:
  path: /
  defaults: {_controller:AppBundle:Default:index}

accueil:
  path: /accueil
  defaults: {_controller:AppBundle:Default:accueil}

accueil_details:
  path: /accueil_details
  defaults: {_controller:AppBundle:Default:accueil_details}

details:
  path: /details/{id}
  defaults: {_controller:AppBundle:Default:details}

recherche:
  path: /recherche
  defaults: {_controller:AppBundle:Default:recherche}

panier:
  path: /panier
  defaults: {_controller:AppBundle:Default:panier}

panier_ajout:
  path: /ajout_panier
  defaults: {_controller:AppBundle:Default:panier_ajout}